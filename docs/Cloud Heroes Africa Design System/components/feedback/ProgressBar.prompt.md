Rounded pill progress bar with an inline % label. Learning-progress rows use orange, ocean and orange fills.

```jsx
<ProgressBar value={87} tone="orange" />
<ProgressBar value={60} tone="dark" height={12} showValue={false} />
```

`tone`: `orange | ocean | blue | dark`. Props: `value`, `height`, `showValue`, `track`.
