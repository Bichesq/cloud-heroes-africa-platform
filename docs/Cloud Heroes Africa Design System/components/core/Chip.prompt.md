Filter / category pill. Selected chips go near-black; category tags can be tinted.

```jsx
<Chip selected>All</Chip>
<Chip>DevOps</Chip>
<Chip tone="dark" size="sm">Cloud Security</Chip>
```

`selected` fills eclipse black. `tone`: `default | dark | ocean | orange`. `size`: `sm | md | lg`.
