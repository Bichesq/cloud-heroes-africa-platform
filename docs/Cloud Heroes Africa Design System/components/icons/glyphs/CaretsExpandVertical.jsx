import { Icon } from '../Icon.jsx';

/** CaretsExpandVertical icon (glyph "expand-vertical" from the CHA icon set). Paints with currentColor. */
export function CaretsExpandVertical(props) {
  return <Icon name="expand-vertical" {...props} />;
}
