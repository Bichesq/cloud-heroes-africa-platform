import { Icon } from '../Icon.jsx';

/** CloseLine icon (glyph "close-line" from the CHA icon set). Paints with currentColor. */
export function CloseLine(props) {
  return <Icon name="close-line" {...props} />;
}
