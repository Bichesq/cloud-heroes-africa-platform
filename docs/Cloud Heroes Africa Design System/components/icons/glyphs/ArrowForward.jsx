import { Icon } from '../Icon.jsx';

/** ArrowForward icon (glyph "arrow-forward" from the CHA icon set). Paints with currentColor. */
export function ArrowForward(props) {
  return <Icon name="arrow-forward" {...props} />;
}
