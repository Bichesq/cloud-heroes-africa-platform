import { Icon } from '../Icon.jsx';

/** PersonLine icon (glyph "person-line" from the CHA icon set). Paints with currentColor. */
export function PersonLine(props) {
  return <Icon name="person-line" {...props} />;
}
