import { Icon } from '../Icon.jsx';

/** CircleDollar icon (glyph "circle-dollar" from the CHA icon set). Paints with currentColor. */
export function CircleDollar(props) {
  return <Icon name="circle-dollar" {...props} />;
}
