import { Icon } from '../Icon.jsx';

/** Envelope icon (glyph "mail" from the CHA icon set). Paints with currentColor. */
export function Envelope(props) {
  return <Icon name="mail" {...props} />;
}
