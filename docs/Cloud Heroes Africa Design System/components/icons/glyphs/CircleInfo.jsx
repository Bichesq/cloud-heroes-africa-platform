import { Icon } from '../Icon.jsx';

/** CircleInfo icon (glyph "info" from the CHA icon set). Paints with currentColor. */
export function CircleInfo(props) {
  return <Icon name="info" {...props} />;
}
