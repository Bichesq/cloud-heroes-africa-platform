import { Icon } from '../Icon.jsx';

/** Menucard icon (glyph "menucard" from the CHA icon set). Paints with currentColor. */
export function Menucard(props) {
  return <Icon name="menucard" {...props} />;
}
