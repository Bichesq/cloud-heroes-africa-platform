import { Icon } from '../Icon.jsx';

/** ChartPie icon (glyph "chart-pie" from the CHA icon set). Paints with currentColor. */
export function ChartPie(props) {
  return <Icon name="chart-pie" {...props} />;
}
