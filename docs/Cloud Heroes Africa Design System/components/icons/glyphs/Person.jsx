import { Icon } from '../Icon.jsx';

/** Person icon (glyph "person" from the CHA icon set). Paints with currentColor. */
export function Person(props) {
  return <Icon name="person" {...props} />;
}
