import { Icon } from '../Icon.jsx';

/** LogoApple icon (glyph "logo-apple" from the CHA icon set). Paints with currentColor. */
export function LogoApple(props) {
  return <Icon name="logo-apple" {...props} />;
}
