import { Icon } from '../Icon.jsx';

/** Ellipsis icon (glyph "ellipsis" from the CHA icon set). Paints with currentColor. */
export function Ellipsis(props) {
  return <Icon name="ellipsis" {...props} />;
}
