import { Icon } from '../Icon.jsx';

/** Binoculars icon (glyph "binoculars" from the CHA icon set). Paints with currentColor. */
export function Binoculars(props) {
  return <Icon name="binoculars" {...props} />;
}
