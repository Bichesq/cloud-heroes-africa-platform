import { Icon } from '../Icon.jsx';

/** ChevronLeft icon (glyph "chevron-left" from the CHA icon set). Paints with currentColor. */
export function ChevronLeft(props) {
  return <Icon name="chevron-left" {...props} />;
}
