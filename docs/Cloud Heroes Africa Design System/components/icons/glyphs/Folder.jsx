import { Icon } from '../Icon.jsx';

/** Folder icon (glyph "folder" from the CHA icon set). Paints with currentColor. */
export function Folder(props) {
  return <Icon name="folder" {...props} />;
}
