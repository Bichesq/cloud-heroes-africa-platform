import { Icon } from '../Icon.jsx';

/** Magnifier icon (glyph "search" from the CHA icon set). Paints with currentColor. */
export function Magnifier(props) {
  return <Icon name="search" {...props} />;
}
