import { Icon } from '../Icon.jsx';

/** CirclesDiamond icon (glyph "diamond" from the CHA icon set). Paints with currentColor. */
export function CirclesDiamond(props) {
  return <Icon name="diamond" {...props} />;
}
