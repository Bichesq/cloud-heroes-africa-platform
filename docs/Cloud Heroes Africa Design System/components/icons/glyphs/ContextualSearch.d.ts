import * as React from 'react';
export interface ContextualSearchProps extends React.SVGProps<SVGSVGElement> {
  /** px size (width & height). Default 20. */
  size?: number | string;
}
export declare function ContextualSearch(props: ContextualSearchProps): JSX.Element;
