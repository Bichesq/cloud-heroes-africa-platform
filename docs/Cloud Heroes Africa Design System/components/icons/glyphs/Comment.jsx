import { Icon } from '../Icon.jsx';

/** Comment icon (glyph "comment" from the CHA icon set). Paints with currentColor. */
export function Comment(props) {
  return <Icon name="comment" {...props} />;
}
