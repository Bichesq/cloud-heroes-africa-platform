import { Icon } from '../Icon.jsx';

/** ChevronDown icon (glyph "chevron-down" from the CHA icon set). Paints with currentColor. */
export function ChevronDown(props) {
  return <Icon name="chevron-down" {...props} />;
}
