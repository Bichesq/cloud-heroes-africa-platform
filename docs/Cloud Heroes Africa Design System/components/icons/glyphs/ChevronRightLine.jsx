import { Icon } from '../Icon.jsx';

/** ChevronRightLine icon (glyph "chevron-right-line" from the CHA icon set). Paints with currentColor. */
export function ChevronRightLine(props) {
  return <Icon name="chevron-right-line" {...props} />;
}
