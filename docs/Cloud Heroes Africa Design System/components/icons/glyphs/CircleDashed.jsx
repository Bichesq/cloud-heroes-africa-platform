import { Icon } from '../Icon.jsx';

/** CircleDashed icon (glyph "circle-dashed" from the CHA icon set). Paints with currentColor. */
export function CircleDashed(props) {
  return <Icon name="circle-dashed" {...props} />;
}
