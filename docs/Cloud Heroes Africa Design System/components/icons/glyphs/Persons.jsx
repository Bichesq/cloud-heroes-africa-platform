import { Icon } from '../Icon.jsx';

/** Persons icon (glyph "persons" from the CHA icon set). Paints with currentColor. */
export function Persons(props) {
  return <Icon name="persons" {...props} />;
}
