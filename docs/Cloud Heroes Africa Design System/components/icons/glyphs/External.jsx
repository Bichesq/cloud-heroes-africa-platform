import { Icon } from '../Icon.jsx';

/** External icon (glyph "external" from the CHA icon set). Paints with currentColor. */
export function External(props) {
  return <Icon name="external" {...props} />;
}
